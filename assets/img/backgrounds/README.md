# Background Image

The website's repeating honeycomb background is stored at:

- `assets/img/backgrounds/honeycomb-tile.png`

Keep the filename and path unchanged unless every CSS reference is updated at the same time.
